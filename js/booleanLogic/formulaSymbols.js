/**
 * Represents the symbols that are used in {@link Formula#toString}.
 * @type {{not: string, equivalence: string, or: string, and: string, implication: string}}
 */
export const FORMULA_SYMBOLS = {
    not: '\\bar',
    and: '',
    or: '\\lor',
    implication: '\\implies',
    equivalence: '\\iff',
}